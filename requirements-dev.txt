# Lumos development and CI dependencies.
#
# Deliberately small. No model SDK appears here: the test suite runs against the
# mock provider and must never need a GPU or a credential (ADR-003).

# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Database
psycopg[binary]==3.2.3

# Testing
pytest==8.3.4
httpx==0.28.1

# PDF inspection — used by scripts/catalog_sources.py to decide the ingest route.
pdfplumber==0.11.4

# OCR — Student Book 1 has no text layer on any of its 225 pages, and the WPH11
# and WPH13 examiner reports embed subset fonts with no ToUnicode CMap. Needs
# the tesseract binary on PATH as well.
pypdfium2==5.13.0
pytesseract==0.3.13
